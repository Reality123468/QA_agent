# 企业内部智能问答系统 — 面试问答准备

> 项目：企业内部规章制度与技术文档智能问答系统（RAG + LangGraph Agent）
> 定位：准大四实习生 / 简历项目
> 技术栈：Vue3 + Spring Boot（Java 21）+ FastAPI（Python 3.11）/ LangChain / LangGraph + Qdrant + MinIO + MySQL + DeepSeek
>
> 使用建议：先背 **第二部分（Q1–Q20 基础问答）** 建立整体认知；面试被追问时，用 **第一部分（3 个核心亮点的追问版）** 展开技术细节。能讲出代码文件名、函数名、参数值的回答最容易拿分。

---

# 第一部分：3 个核心亮点（追问深入版）

这 3 个亮点是面试官最可能深挖的方向，按"基础介绍 → 连环追问"组织，建议重点准备。

---

## 亮点 1：混合检索（BM25 + 向量语义）+ RRF 融合 —— RAG 质量核心

### 基础介绍（一句话版）

用户在 `rag/retriever.py` 的 `hybrid_search()` 中，用线程池**并行**做两路召回：一路 Qdrant 向量语义检索、一路 BM25 关键词检索，各取 Top-20，再用 **RRF（Reciprocal Rank Fusion，倒数排名融合）** 按排名位次融合成统一的 Top-10，最后由 Reranker 精排到 Top-5 送入 LLM。

- 向量召回 `top_k=20`、BM25 召回 `top_k=20`，融合后 `top_k=10`；
- RRF 平滑参数 `RRF_K = 60`；
- 融合公式：`score = 1/(k + rank_dense) + 1/(k + rank_sparse)`；
- 以 `(doc_id, chunk_index)` 作为唯一键合并两路结果。

### 追问 1：RRF 公式里 k 是干什么的？为什么取 60？

**答**：`k` 是平滑常数，作用是**削弱排名差异的极端化**。`1/(k+rank)` 意味着排第 1 名贡献 `1/(k+1)`，排第 2 名贡献 `1/(k+2)`。

- `k` 越大：排名靠前和靠后的分数差距被"压平"，更看重"是否出现在结果里"而非"排第几"；
- `k` 越小：越强调第一名，头部集中。

`k=60` 是信息检索领域的经典经验值（Cormack et al. 2009 的实验结论），适合 rank 范围在几十以内的场景。它不是拍脑袋，而是大量实验表明 60 附近对多种检索器组合最稳健。**面试加分点**：如果检索器只有两路、且都希望头部权重更高，可以适当调小 k；但默认 60 最安全。

### 追问 2：BM25 和向量召回的原始分数量级、分布完全不同，为什么能直接融合？

**答**：这正是 RRF 相比"分数加权"的**核心优势**——RRF **只用 rank（排序位次），不用原始 score**。

- 向量相似度（cosine）范围在 [-1,1] 或 [0,1]，BM25 分数可能几百上千，两者**不可直接相加或加权平均**，否则分数大的检索器会完全主导；
- 要做加权融合，必须先对两路分数做 min-max 或 z-score 归一化，工程复杂且对分布敏感；
- RRF 完全规避了这个问题：它只看"这个文档在第 i 路排第几名"，对分数的 scale 和分布**不敏感**，鲁棒性极高。

所以 RRF 是"懒人但有效"的多路召回融合方案，也是现在 RAG 工程里的标配。

### 追问 3：能不能现场写一下 RRF 融合的核心逻辑？

**答**：核心在 `retriever.py` 的 `_rrf_fusion()`，伪代码（Python 风格）：

```python
def rrf_fusion(dense_hits, sparse_hits, k=60):
    rrf = {}          # (doc_id, chunk_index) -> 累计分数
    hit_map = {}      # (doc_id, chunk_index) -> 命中文档内容
    # 第一路（向量）
    for rank, hit in enumerate(dense_hits, start=1):
        key = (hit["doc_id"], hit["chunk_index"])
        rrf[key] = rrf.get(key, 0) + 1.0 / (k + rank)
        hit_map.setdefault(key, hit)
    # 第二路（BM25）
    for rank, hit in enumerate(sparse_hits, start=1):
        key = (hit["doc_id"], hit["chunk_index"])
        rrf[key] = rrf.get(key, 0) + 1.0 / (k + rank)
        hit_map.setdefault(key, hit)
    # 按分数降序返回
    return [ {**hit_map[key], "score": round(rrf[key], 6)}
             for key in sorted(rrf, key=rrf.get, reverse=True) ]
```

注意两个细节：(1) 用 `setdefault` 保证同一文档只保留一份内容；(2) 分数是**累加**两路贡献，所以"两路都排前面"的文档分数最高，符合直觉。

### 追问 4：检索前过滤（Pre-filtering）和检索后过滤（Post-filtering）有什么区别？你为什么选前者？

**答**：这是企业 RAG 权限实现的**关键决策点**。

- **Pre-filtering（检索前过滤）**：在 Qdrant 查询时通过 `Filter(must=[...])` 条件，在向量索引阶段就排除无权限的 chunk。用户根本不会召回越权内容，安全性最好，也不会浪费 top_k 名额。
- **Post-filtering（检索后过滤）**：先召回 Top-N，再在应用层裁掉无权限的。问题有两个：(1) 容易把 quota 浪费在越权文档上，导致有权限的结果被截断；(2) 越权片段可能已经进入检索上下文，存在**信息泄露风险**。

我在 `_qdrant_search()` 里用的是 Pre-filtering：

```python
must_conditions = [
    FieldCondition(key="department", match=MatchAny(any=[department, "全部"])),
]
if security_level == "内部":
    must_conditions.append(
        FieldCondition(key="security_level", match=MatchExcept(except=["机密"]))
    )
# client.query_points(query_filter=Filter(must=must_conditions), ...)
```

过滤发生在检索之前，保证用户只能召回有权限的文档片段。

---

## 亮点 2：LangGraph Agent 的 ReAct 循环 + 流式推理 —— AI 智能体核心亮点

### 基础介绍（一句话版）

Agent 用 LangGraph 的 `StateGraph`（`agent/graph.py`）构建 **ReAct 推理循环**：`agent_node`（LLM 决策是否调工具）↔ `tools_node`（执行工具）循环，`recursion_limit=10` 防死循环。流式输出不用 `astream_events(v2)`，而用 `graph.astream(stream_mode="values")`，通过比对消息增量把推理过程映射成 SSE 事件（`thought/action/observation/answer`）。

### 追问 1：为什么用 LangGraph 而不是直接写 while 循环调 LLM？

**答**：直接写 `while True: call_llm(); if has_tool_calls: call_tool()` 当然也能跑，但 LangGraph 提供的是**工程化收益**：

1. **显式状态管理**：`AgentState`（`TypedDict`）把 `messages / department / security_level / retrieved_docs / summary` 结构化，避免散落的全局变量；
2. **可视化与可观测**：图结构天然可画出来，配合 LangSmith 能追踪每一步；
3. **checkpointer**：内置 `MemorySaver`/`RedisSaver` 支持多轮对话状态持久化（`thread_id` 路由）；
4. **流式 API**：`astream` 已经处理好节点间状态流转，不用自己攒 buffer。

一句话：**手写循环是"能跑"，LangGraph 是"能维护、能扩展、能观测"**，对一个要写进简历、可能被追问架构的项目，前者更稳妥。

### 追问 2：流式为什么必须用 `stream_mode="values"` 而不是 `astream_events(v2)`？

**答**：这是项目里一个**真实的坑**（`CLAUDE.md` 专门记了）。

`astream_events(v2)` 只在 LangChain 的 **ChatModel 原生包装器** 上触发 `on_chat_model_*` 事件。但我的 `agent_node` 里调的是 `chat_sync()`——一个**直接包 OpenAI 客户端的函数**（`llm/deepseek_client.py`），不是 LangChain 的 `ChatOpenAI` 包装。所以这些事件**根本不冒泡**，用 v2 拿不到任何 token。

改用 `stream_mode="values"`：每个节点执行完后，它返回**完整的累积 state**（全部 messages）。我维护一个 `msg_count`，对比新旧消息数得到增量：
- 新增 `AIMessage` 且带 `tool_calls` → 映射成 `action` 事件；
- 新增 `ToolMessage`（有 `tool_call_id`）→ 映射成 `observation` 事件；
- 新增 `AIMessage` 且无 `tool_calls` → 这是最终答案，逐字符吐成 `answer`。

这是"绕开框架限制、靠增量 diff 自己解析"的解法，面试官听到这个通常很认可。

### 追问 3：能不能手写一下 StateGraph 的构建？

**答**：参考 `agent/graph.py`，核心结构：

```python
from langgraph.graph import StateGraph, END
from agent.state import AgentState
from agent.nodes import agent_node, tools_node

def _should_continue(state):
    last = state["messages"][-1]
    if getattr(last, "tool_calls", None):
        return "tools"
    return END

def build_agent_graph():
    wf = StateGraph(AgentState)
    wf.add_node("agent", agent_node)
    wf.add_node("tools", tools_node)
    wf.set_entry_point("agent")
    wf.add_conditional_edges("agent", _should_continue,
                             {"tools": "tools", END: END})
    wf.add_edge("tools", "agent")      # 工具执行完回到 agent
    return wf.compile()
```

调用时：`graph.astream(state, config={"recursion_limit": 10}, stream_mode="values")`。

### 追问 4：`recursion_limit=10` 是怎么防止 Agent 死循环的？

**答**：ReAct 循环理论上是 `while` 直到 LLM 不再请求工具。但 LLM 可能陷入"调工具→结果不满意→再调同一工具"的循环。LangGraph 的 `recursion_limit` 限制**节点跳转总次数**，超过 10 次直接中断并抛异常，外层 `_agent_stream` 捕获后返回"该问题过于复杂，我暂时无法回答"，并触发三级降级。配合整体 `AGENT_STREAM_TIMEOUT=120s` 的双重保险，避免无限占用资源和 token。

### 追问 5：多轮对话状态怎么管理？为什么没用 MemorySaver？

**答**：目前的设计是**服务端无状态**：跨轮上下文由**前端每轮发送全量 `history`**（`agent_routes.py` 里把 history 转成 `HumanMessage`/`AIMessage` 拼到初始 state）。好处是实现简单、不依赖共享存储、易于水平扩展。代价是请求体随对话变长（已用 token 预算 + 历史摘要压缩缓解）。

代码注释里也写了演进方向：未来若要多实例共享 / 服务端托管历史，可换 `RedisSaver` 并用 `thread_id` 路由恢复状态——这正好体现"我清楚当前方案的 trade-off"。

---

## 亮点 3：企业级容错（三级降级）+ 检索前权限过滤 —— 工程与安全的落地核心

### 基础介绍（一句话版）

稳定性上，`agent_routes.py` 的 `_agent_stream` 实现了**三级自动降级**：Agent 异常/超时 → 降级 RAG 模式 → 再失败降级 search-only → 最终兜底文案，每次降级发 `downgrade` SSE 事件。工具层有 `CircuitBreaker`（每工具独立熔断）+ `tenacity` 指数退避重试 + 15s 单工具超时。安全性上，权限过滤**前移到 Qdrant 检索**（`MatchAny` 部门 + `MatchExcept` 排除机密），并用 `contextvars` 在请求内传递安全级别、注入安全前缀。

### 追问 1：三级降级怎么保证"不丢答案"且上下文连续？

**答**：关键是**降级时复用原始输入、逐层兜底**：

1. Tier1 Agent 失败（异常 / 超过 120s / 无答案）→ 进入 Tier2；
2. Tier2 用**同一个 `question`** 调 `answer_with_rag()`，RAG 仍基于检索，用户至少能拿到"有出处"的回答；
3. Tier2 也失败 → Tier3 `search_only` 只返回检索片段，不调 LLM，至少给用户原文参考；
4. 全失败 → Tier4 固定兜底文案。

每次切换都 `yield downgrade` 事件，前端能显示"已切换模式"。所以**任何单点故障都不会白屏**，用户总能获得某种程度的响应。这是"可用性优先"的设计哲学。

### 追问 2：CircuitBreaker 工作原理？和 retry 怎么配合？

**答**：两者解决不同层面的问题，在 `nodes.py` 里是**组合使用**的：

- **Retry（`tenacity`）**：处理**瞬时故障**（网络抖动、LLM 限流）。`_execute_with_retry` 用指数退避 `wait_exponential(1s→2s→4s)`，最多 3 次。目的是"再试一次可能就成功了"。
- **CircuitBreaker**：处理**持续性故障**（下游真的挂了）。每个工具（`search_knowledge/search_employee/get_doc_detail`）有独立熔断器。它统计失败率，当失败过多进入 **OPEN（开路）** 状态，此时**直接快速失败**（返回友好提示），不再打下游，给下游喘息时间；过一段时间进入 **HALF-OPEN** 试探一个请求，成功则恢复 CLOSED。

配合逻辑：先 retry 尝试恢复瞬时错误；若 retry 耗尽仍失败，错误冒泡到 breaker，breaker 累计失败达到阈值后开路，避免对已知故障服务持续施压（**雪崩防护**）。

### 追问 3：Qdrant 的权限过滤具体怎么写？普通员工为什么看不到"机密"？

**答**：在 `retriever.py` 的 `_qdrant_search()` 和 `get_document_chunks()` 里，根据 Java 传来的角色决定 `security_level`：

```python
# agent_routes.py：角色 -> 安全级别
security_level = "内部"
if x_user_role in ("ROLE_LEADER", "ROLE_ADMIN", "ROLE_HR"):
    security_level = "机密"

# retriever.py：内部用户排除机密
if security_level == "内部":
    must_conditions.append(
        FieldCondition(key="security_level", match=MatchExcept(except=["机密"]))
    )
must_conditions.append(
    FieldCondition(key="department", match=MatchAny(any=[department, "全部"]))
)
```

即：普通员工（`ROLE_EMPLOYEE`/`ROLE_LEADER`）`security_level="内部"`，检索时 `MatchExcept(["机密"])` 把机密文档挡在召回之外；主管/HR/管理员 `security_level="机密"`，可见全部。部门用 `MatchAny([用户部门, "全部"])` 实现"本部门 + 公共文档"的可见性。

### 追问 4：为什么用 `contextvars` 传安全级别，而不是函数参数层层透传？

**答**：这是为了避免一个**隐蔽 bug**。

`agent_node` 在 ReAct 循环里会被**多次调用**。如果我把安全前缀直接 `mutate` 进 `state["messages"]`，下一次循环前缀会**累积叠加**（越叠越长）。所以代码里是构造 `temp_messages = list(messages)` 浅拷贝，只给最后一条 `HumanMessage` 注入前缀，**绝不改原始 state**。

而 `tools_node` 执行工具时，工具函数（`tools.py`）需要知道自己当前的安全级别来做检索过滤。如果用参数层层传，要从 route → graph → node → tool 一路透传，非常啰嗦。用 `contextvars.ContextVar`（见 `agent/context.py`）：

- 请求入口 `current_security_level.set(seclevel)` 设置；
- 工具内部 `seclevel = current_security_level.get()` 读取；

它是**请求级、协程安全的上下文**，不需要改函数签名，也不会因为 ReAct 循环多次调用而出错。这是 Python 异步编程里"请求上下文传递"的标准做法（类似 Flask 的 `g`、FastAPI 的 `request.state`）。

---

# 第二部分：完整面试问答（基础版 Q1–Q20）

> 下面是项目整体到各模块的答疑，作为面试开场和常规追问的素材。

## 一、项目整体 & 架构

**Q1：先做个自我介绍，并介绍一下你这个项目。**

A：我是准大四学生，这个项目是我做的一个**企业内部智能问答系统**，目标是让员工用自然语言查询公司规章制度和技术文档。它不是简单套个大模型，而是用 **RAG + Agent** 来保证答案"有据可依"。

整体是**前后端分离 + Java/Python 双栈**的架构：前端 Vue3，业务底座用 Java Spring Boot（认证、文档管理、会话、审计），AI 能力用 Python FastAPI + LangChain/LangGraph 实现（RAG 检索、LangGraph Agent）。存储层是 MySQL + Qdrant 向量库 + MinIO 对象存储，全部用 Docker Compose 一键部署。我独立完成了从需求、设计到编码、部署的完整流程。

**Q2：为什么选 Java + Python 混合架构，而不是纯 Java 或纯 Python？**

A：主要是**能力互补**和**面试价值**两点考虑：
- AI 生态：LangChain/LangGraph 在 Python 侧远比 Java（LangChain4j）成熟，本地加载 Reranker、做 ReAct 循环更顺手；
- 工程能力：企业级业务（认证、权限、审计、文档管理）用 Spring Boot 最稳，Spring Security、事务、JPA 都是现成轮子；
- 解耦：两个服务通过 HTTP + 内部 API Key 通信，可独立部署、独立扩容。
所以最终让"AI 逻辑在 Python，业务逻辑在 Java"，两边都覆盖到，简历上也更有亮点。

**Q3：一次问答请求的完整数据流是怎样的？**

A：以 Agent 模式为例：
1. 浏览器把 `question/mode/history` 通过 **SSE（POST）** 发给 Java Chat 服务；
2. Java 校验 JWT，把用户角色/部门塞进 `X-User-Role`、`X-User-Department` 等 Header，用 **WebClient** 转发给 Python 的 `/api/agent/chat/stream`；
3. Python 路由层判断 mode=agent，进入 `graph.astream()` 的 **ReAct 循环**，期间通过工具调用 Qdrant 检索；
4. Python 把推理过程（`thought/action/observation`）和答案（`answer`）以 SSE 逐事件推回 Java，Java **透传**给前端逐字渲染；
5. 流结束后，Java 把问答、引用、Agent 步骤、Token 用量落库到 `audit_log` 和 `chat_message`，完成审计。

## 二、RAG 技术深度

**Q4：RAG 链路是怎么设计的？分块策略是什么？**

A：RAG 是"检索增强生成"。我的流水线在 `rag_chain.py`：`检索 → Rerank 精排 → 构造 Prompt → LLM 流式生成 → 溯源引用`。

分块是 RAG 的质量瓶颈，我做了**差异化策略**（见 `splitter.py`）：
- Markdown（规章制度）用 `MarkdownHeaderTextSplitter` 按 `##/###` 标题层级切，保留章节结构；
- PDF/DOCX/TXT 用 `RecursiveCharacterTextSplitter`，按 **800 字符、150 字符 overlap** 切。
- 每个 chunk 入库时附加 `doc_id/title/department/security_level/chunk_index/heading` 等 metadata，既用于**权限过滤**也用于**溯源**。

**Q5：检索为什么用"混合检索"？具体怎么融合的？**

A：单一向量检索容易漏掉关键词精确匹配（比如"年假 5 天"里的数字），所以我把**BM25 关键词召回**和**Qdrant 向量语义召回**做了双路并行，再用 **RRF（Reciprocal Rank Fusion）** 融合，公式是 `score = 1/(k+rank_dense) + 1/(k+rank_sparse)`，k=60。两路各取 Top-20，融合后取 Top-10，再由 Reranker 精排到 Top-5 送进 Prompt。这样兼顾了"字面命中"和"语义相似"。

**Q6：Embedding 模型用的是什么？有没有考虑过降级？**

A：理想是 DeepSeek 或本地 BGE-M3，但实际环境（国内、HuggingFace 被墙）里 BGE-M3 的 `encode()` 会卡死。所以我做了**三级降级**（`embedder.py`）：
1. 优先 DeepSeek Embedding API；
2. 失败回退本地 HF 模型；
3. 再失败用 sklearn `HashingVectorizer`（零依赖、384 维）兜底。
保证任何环境下系统都能跑起来——这是个工程稳健性的设计点。

**Q7：怎么控制 LLM 不"胡说"（幻觉）？**

A：两层约束：
- **Prompt 层面**：系统提示明确要求"仅根据参考文档回答，找不到就说无法回答，并引用文档名和章节"；
- **架构层面**：答案必须基于检索回来的 context，并且前端会展示 `citation` 溯源引用，用户可以点开核对原文。
另外 RAG 模式不调用工具、只做生成，本身就是对 Agent 不可用时"有据可依"的降级方案。

## 三、LangGraph Agent

**Q8：你的 Agent 是怎么实现 ReAct 推理循环的？**

A：用 LangGraph 的 `StateGraph`（`graph.py`）构建：
- 两个节点：`agent_node`（调 LLM 决策）和 `tools_node`（执行工具）；
- 入口是 `agent`，`agent` 之后用**条件边** `_should_continue` 判断最后一条消息是否带 `tool_calls`：有就走 `tools`，没有就 `END` 直接给最终答案；
- `tools` 执行完再回到 `agent`，形成"思考→行动→观察"循环，`recursion_limit=10` 防止死循环。
工具是 `search_knowledge / search_employee / get_doc_detail` 三个，用 LangChain `@tool` 装饰器把函数转成 OpenAI function-calling 格式。

**Q9：Agent 怎么把推理过程"流式"展示给前端？这是难点吗？**

A：是难点之一。我一开始想用 LangGraph 的 `astream_events(v2)`，但发现它只对 LangChain 的 ChatModel 包装器触发事件，而我的 `agent_node` 直接调了 `chat_sync()`（一个 OpenAI 客户端包装器），事件根本不冒泡。

所以改用 **`graph.astream(stream_mode="values")`**：每轮循环后它返回**完整的累积 state**，我通过维护 `msg_count` 对比新旧消息数，新增的 AIMessage（带 tool_calls）映射成 `action` 事件，ToolMessage 映射成 `observation` 事件，最终无 tool_calls 的 AIMessage 作为 `answer` 逐字符吐出。

## 四、容错、降级与稳定性

**Q10：Agent 出错或超时怎么办？**

A：我在 `agent_routes.py` 里实现了**三级自动降级**（`_agent_stream`）：
- Tier1 Agent 异常/超时（整体 120s）→ 降级到 **RAG 模式**；
- Tier2 RAG 也失败 → 降级到 **search-only**（只返回检索片段，不调 LLM）；
- Tier3 全失败 → 返回兜底文案"系统暂时无法处理…"。
每次降级都发一个 `downgrade` SSE 事件，前端能展示"已切换模式"。这样**任一环节挂了用户都不会白屏**。

**Q11：工具调用本身的可靠性怎么保证？**

A：两点（见 `nodes.py`）：
- **超时**：单个工具 `asyncio.wait_for(15s)` 上限；
- **重试 + 熔断**：用 `tenacity` 做指数退避重试（1s→2s→4s，最多 3 次），每个工具配一个**熔断器** `CircuitBreaker`，连续失败就直接开路返回友好提示，避免雪崩。

**Q12：长对话会不会爆 Token？怎么处理？**

A：会，所以我做了 **Token 预算管理**（`deepseek_client.py`）：
- 用 tiktoken(`cl100k_base`) 估算消息 token 数，`TOKEN_BUDGET=8000`；
- 超预算时先调 `summarize_history()` 让 flash 模型把较早历史压缩成摘要（保留最近 3 条原文），还不行就 `ensure_token_budget()` 从头部硬截断。用户基本无感知。

**Q13：多轮对话里用户追问"那个也一样"，模型怎么知道指什么？**

A：这是**查询改写**（`query_rewriter.py`）。我先用规则预判：问题 <10 字、含指代词（这/那/它）、含省略标记（也/还/同样）才触发改写，避免每次都白调 LLM。命中后用 DeepSeek flash 把追问结合最近 3 轮历史补全成独立检索语句，再拿去检索。

## 五、Java 工程 & 权限

**Q14：Java 侧怎么消费 Python 的 SSE 流并落库？**

A：用 **Spring WebFlux 的 WebClient**（响应式），`bodyToFlux(ChatResponse.class)` 把 SSE 流逐事件转成对象。在 `doOnNext` 里按事件类型分流：answer 累加到 buffer、citation 收集、thought/action/observation 记成 Agent 步骤、token_usage 写入审计；`doOnComplete` 时把答案和引用落库；`doOnError` 捕获异常并记审计。整条链路是**响应式非阻塞**的，不会因长连接阻塞线程。

**Q15：前端的 SSE 为什么不用 EventSource？**

A：因为 `EventSource` 只支持 **GET**，而我们的对话请求需要带 `question/history/mode` 请求体。所以前端用 **`fetch + ReadableStream`** 手动解析 SSE 文本（`data: {...}\n\n`），配合 `AbortController` 支持中断。

**Q16：企业权限（RBAC）在 RAG 里怎么落地的？这是公认难点。**

A：核心是**检索前过滤（Pre-filtering）**，而不是检索后裁切：
- Java 把用户角色/部门通过 Header 传给 Python；
- Python 在 Qdrant 检索时直接构造 `Filter`：`department` 用 `MatchAny([用户部门, "全部"])`，`security_level` 普通员工用 `MatchExcept(["机密"])` 排除机密文档；
- 此外在 Agent 的用户消息前注入**安全前缀**（如"禁止泄露机密内容"），用 `contextvars` 在请求内传递安全级别，避免在 ReAct 循环里反复 mutate 消息导致前缀累积。

**Q17：文档索引是同步的还是异步的？进度怎么通知前端？**

A：异步。Java 用 `@Async` 调 Python `/api/agent/index`，Python 处理完分块、向量化、入库后，回调 Java 的进度接口，Java 再通过 **WebSocket（`/ws/index-progress`）** 把进度实时推给浏览器，用户能看到"索引中/已完成"。

## 六、踩坑 & 反思

**Q18：这个项目里你踩过最深的坑是什么？**

A：两个印象深的：
1. **LangGraph 流式事件不触发**：前面说的，误用 `astream_events(v2)` 拿不到自定义客户端的事件，debug 半天才定位，最后改用 `stream_mode="values"` 自己比对消息增量。这让我学到"用框架前一定要搞清楚它的事件触发条件"。
2. **跨语言协议细节**：Java（camelCase）和 Python（snake_case）对接时，Pydantic v2 会因为 `List` 字段收到 `null` 直接 422，必须让 Java 发 `[]`；还有 WebClient 拼请求体时 key 必须用 snake_case。这些"联调小坑"花了我不少时间，也让我更重视接口契约和前后端类型对齐。

**Q19：你是怎么评估 RAG 效果的？**

A：我设计了一套评估体系（参考 RAGAS 指标）：检索侧看 Hit Rate（Top-5 命中率，目标>85%）、MRR；答案侧看 Faithfulness（忠实度，答案 claim 是否在 context 有支撑）和 Answer Relevancy。还专门构造了"知识库外无关问题"测**拒答率**。测试集覆盖事实型、跨文档推理、权限边界、无关问题四类，大约 50–100 条。

**Q20：如果让你继续优化，你会做哪几件事？**

A：我会提三点：
1. **Reranker 升级**：现在用 LLM listwise 精排成本和延迟偏高，可以换成轻量本地 Cross-Encoder；
2. **多轮会话服务化**：目前跨轮上下文靠前端每轮发全量 history，后续可以用 RedisSaver 在服务端托管 `thread_id`，减轻请求体；
3. **可观测性**：接 LangSmith 把 Agent 的 tool_call 链路、Token 消耗做成可视化，方便定位"为什么检索不到"这类问题。
